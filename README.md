# Turn.io and OpenSRP integration

Example Turn.io and OpenSRP integration app

```shell
$ yarn install
$ DEBUG="turn:*" \
    OPENSRP_ENDPOINT="https://your.smartregister.org" &&
    OPENSRP_USERNAME="username" &&
    OPENSRP_PASSWORD="password" && yarn serve
```
